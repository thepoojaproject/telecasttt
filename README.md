# Telecast APK - Fixed

Login: `Neelam`
Password: `Neelam143`

Footer: **Made with ❤️ for Neelam**

## Fixed GitHub Actions issue
The project now uses `capacitor.config.json` instead of `capacitor.config.ts`, so Capacitor does not require TypeScript during `npx cap add android`.

The workflow uses `npm ci` with the included `package-lock.json`.
